# 03 — CM Operator: บันทึกการใช้วัตถุดิบ + ชั่งของเสีย

**สำหรับ**: CM (Compounding / Mixing) Operator — คนห้อง Mixing
**ใช้เวลาเรียน**: ~25 นาที
**ต้องมีก่อน**: บัญชี user + role = CM Operator
**สำคัญ**: เอกสารนี้อธิบาย **feature ใหม่ล่าสุด W18** (31 ก.ค. 2026) — ไม่เหมือนของเดิม 100%

---

## ⚡ สรุปสั้น — สิ่งที่เปลี่ยน W18

| หัวข้อ | ของเดิม | ✨ W18 ใหม่ |
|---|---|---|
| **ตัด HIBE / VERP / UNBW** | ต้องเลือก Lot + กรอกมือทีละตัว | **AUTO** — ระบบตัดให้ตามสูตร BOM × ยอดจริง |
| **ตัด ROH (Raw Material)** | ทำมือ | เหมือนเดิม — เลือก Lot + กรอกจำนวน |
| **ชั่งน้ำหนักของเสีย** | ทำที่หน้า Tech เท่านั้น | **CM ทำได้เอง** ผ่านปุ่ม ⚖️ ในหน้าตัวเอง |
| **Stock UI (Doc/LD)** | โหลด 250 SQL → ช้ามาก | เร็วขึ้น 100 เท่า (2 SQL) |

---

## ภาพรวม — งานของ CM Operator

```
🌅 เริ่มกะ
  ↓
  1. Login → เลือก WO ที่เครื่องกำลังจะผลิต
  2. ระบบเปิด BOM ของ product นั้น
  3. ตัด ROH ทีละตัว: เลือก Lot + กรอก Qty
  4. HIBE/VERP/UNBW → tick เลย ระบบตัดให้อัตโนมัติ
  5. กดยืนยัน batch → บันทึก stock-out ทั้งหมด
  ↓
⚖️ ระหว่างวัน (ทำได้ตลอด)
  ↓
  6. ชั่งของเสีย → กด ⚖️ → เลือกประเภท + กรอกน้ำหนัก
  ↓
🌆 จบกะ
```

---

## 1️⃣ Login + เลือก WO

### ขั้นตอน
1. Login เข้าระบบ
2. Dashboard แสดง **"เลือกใบสั่งผลิตเพื่อบันทึกการใช้วัตถุดิบ"**
3. เห็น **grid ของเครื่องจักรที่กำลังผลิต** (MachineSelectGrid)
4. คลิกเครื่อง/WO ที่ตัวเอง mixing ให้

### ⚠️ ถ้าไม่เห็น WO
- Operator ยังไม่เริ่ม RunCard → รอ Operator เปิด
- QA ยังไม่อนุมัติ Setup → ตาม QA
- WO ผิดกะ → เช็ค PC

---

## 2️⃣ ดู BOM ของ Product นั้น

### หลังคลิก WO เข้ามา
- หัวข้อ **"ขั้นตอนที่ 2: บันทึกการเบิกจ่ายวัตถุดิบ (Stock-Out)"**
- ด้านบน: `เครื่องจักร: TAHARA-101` · `ผลิตภัณฑ์: <ชื่อ product>`
- ปุ่ม **← กลับไปเลือกใบสั่งผลิต** (มุมซ้ายบน)
- ปุ่ม **⚖️ ชั่งน้ำหนักของเสีย** (ทางขวา — ใช้ได้ตลอด)

### ตาราง BOM มี 7 คอลัมน์
| คอลัมน์ | ความหมาย |
|---|---|
| ☐ ใช้ | Tick เพื่อบันทึกวัตถุดิบตัวนี้ |
| รหัส | Material code (SAP) |
| ชื่อวัตถุดิบ | Material name |
| ประเภท | **ROH** / HIBE / VERP / UNBW · isScrap ระบบซ่อนอยู่แล้ว |
| BOM (Kg) | สูตรมาตรฐาน per FG |
| Lot Number | เลือก Lot (ROH เท่านั้น) |
| ปริมาณที่ใช้จริง (Kg) | ยอดจริง |

### ⚠️ ถ้าเห็น "ไม่พบ BOM สำหรับสินค้านี้"
- แจ้ง PC / Admin — ให้อัปโหลด BOM ก่อน
- **อย่ากดผ่านไปโดยไม่มี BOM** — ระบบจะไม่รู้ว่าใช้อะไรบ้าง

---

## 3️⃣ วิธีตัด ROH (Raw Material) — ทำมือ

**ROH = พลาสติกดิบ / เม็ดพลาสติก · ต้องระบุ Lot ที่ใช้จริง**

### ขั้นตอน
1. หาแถวที่ **ประเภท = ROH**
2. Tick ☐ **ใช้** ในช่องซ้าย → แถวเปิดใช้งาน
3. เลือก **Lot Number** จาก dropdown
   - Dropdown จะแสดงเฉพาะ Lot ที่มี stock พร้อมใช้
   - **ถ้าเห็น "ไม่มี Stock"** สีส้ม → แจ้ง LD/Doc รับของก่อน
4. กรอก **ปริมาณที่ใช้จริง (Kg)** — น้ำหนักจริงที่ตัก/ชั่ง
   - **ไม่ต้องเท่ากับ BOM** — กรอกตามจริง
5. ทำซ้ำสำหรับ ROH ทุกตัวในสูตร

### ⚠️ ข้อควรระวัง
- **เลือก Lot ผิด** = trace ย้อนไม่ได้ → กรอก Lot ให้ตรงกับป้ายจริงที่ถุง
- **ถ้าใช้ 2 Lot** ในการ mix เดียวกัน → ต้องเพิ่มแถวใหม่ (คุย PC ก่อน)
- ตอนนี้ระบบรองรับ **1 Lot ต่อ row** เท่านั้น

---

## 4️⃣ วิธีตัด HIBE / VERP / UNBW — AUTO ✨

**HIBE, VERP, UNBW = สารเสริม / packaging / additive · ระบบตัดให้อัตโนมัติ**

### วิธีสังเกต
- แถวจะมี **AUTO badge สีน้ำเงิน** และ **แถวสีฟ้า**
- ช่อง Lot Number แสดง **"ตัดอัตโนมัติ"** (ไม่ต้องเลือก)
- ปริมาณที่ใช้จริง ระบบ **pre-fill ให้แล้ว** = BOM × ยอดจริงที่ Operator ผลิต

### ขั้นตอน (สั้นมาก)
1. ตรวจว่าแถว AUTO **tick ☐ ใช้** เอาไว้แล้ว (default = ใช้)
2. ตรวจปริมาณที่ pre-fill ให้ — ถ้าตรงกับที่ใช้จริง = ไม่ต้องแก้
3. ถ้าใช้ไม่ตรง (เช่น additive มากกว่าปกติ) → แก้ตัวเลขได้
4. ผ่านไปตัวถัดไป

### ทำไมต้อง AUTO?
- HIBE/VERP/UNBW ปกติใช้ **ตามสูตร BOM ตรง ๆ** ไม่ค่อยเปลี่ยน
- ลดเวลากรอก + ลดโอกาส typo
- **ระบบข้าม stock balance check** — ถ้า Doc/LD บันทึกรับผิด ระบบจะไม่ block
- Doc ต้องดูรายงาน stock balance เอง

### ⚠️ ข้อควรระวัง
- **แม้จะ AUTO ต้องเช็คตัวเลข** ก่อนกดยืนยัน
- ถ้าเห็น **"ไม่พบใน Master"** (สีส้ม) → material code นี้ไม่มีใน master แจ้ง Admin

---

## 5️⃣ กดยืนยัน batch — บันทึกทั้งหมดพร้อมกัน

### ก่อนกดยืนยัน checklist
- [ ] ROH ทุกตัว: เลือก Lot + กรอก Qty จริง
- [ ] HIBE/VERP/UNBW: ตรวจตัวเลขที่ AUTO
- [ ] ปริมาณรวม ~ เท่ากับ shot × น้ำหนัก per shot (roughly)
- [ ] ไม่มีแถวไหน "ไม่มี Stock" ที่ยังต้องใช้

### กดยืนยัน
- ปุ่มยืนยัน (สีน้ำเงิน) — batch submit ทีเดียวทุกแถวที่ tick
- ระบบเรียก `POST /api/cm-operator/stock-out/batch`
- รอ 2-3 วินาที → แจ้ง success/error

### หลังบันทึก
- Stock ทุกตัวหักลดใน DB
- Doc / LD เห็นยอดใน Material Stock Manager ทันที
- PC เห็นใน Variance Report — เทียบ actual vs planned

---

## 6️⃣ ⚖️ ชั่งน้ำหนักของเสีย (ใหม่ W18)

**CM ทำเองได้ ไม่ต้องรอ Tech!**

### เมื่อไหร่ควรชั่ง?
- ระหว่าง mix — วัตถุดิบตกพื้น
- ตัวเสียจาก mixer (over-heat / contaminated)
- Sweep ปลายกะ
- ก่อนล้าง mixer เปลี่ยนสี

### ขั้นตอน
1. กดปุ่ม **⚖️ ชั่งน้ำหนักของเสีย** (มุมขวาบนของ BomStockOutView)
2. Modal เด้ง แสดงหัวข้อ **"ชั่งน้ำหนักของเสีย"**
3. เลือก **ประเภทของเสีย** จาก dropdown — 8 ตัวเลือก:

| ประเภท | ใช้เมื่อ |
|---|---|
| PE ส่งบดใช้ ถุงสีขาว | เศษ PE จะบดกลับใช้เอง |
| PE ส่งบดขาย ถุงสีแดง | เศษ PE ขายให้ผู้รับซื้อไปบด |
| PE ส่งขาย ถุงสีฟ้า | PE ที่ขายเลย ไม่ต้องบด |
| PE เพิร์ส ถุงสีฟ้า | PE เพิร์ส (?) |
| PP ส่งบดใช้ ถุงสีขาว | เศษ PP บดกลับใช้เอง |
| PP ส่งบดขาย ถุงสีแดง | PP ขายให้บด |
| PP ส่งขาย ถุงสีแดง | PP ขายไม่บด |
| PP เพิร์ส ถุงสีแดง | PP เพิร์ส |

4. กรอก **น้ำหนัก (Kg.)** — น้ำหนักที่ชั่งได้จริง
5. กด **ยืนยัน**
6. ข้อมูลลง `scrap_weight_logs` — table เดียวกับที่ Tech ใช้
7. **PC เห็นใน BlowDailyReport Section 5** (NG Summary)

### ⚠️ ข้อควรระวัง
- **เลือกประเภทให้ถูกสี** — ทีมขายเศษใช้ข้อมูลนี้เขียนใบส่งของ
- ชั่งครั้งไหน บันทึกครั้งนั้น — อย่าสะสมชั่งทีเดียวปลายกะ
- ถ้าชั่งผิด → แจ้ง PC/Tech แก้ (audit log บันทึกทั้งหมด)

---

## 🎨 คู่มืออ่านสี — ระบบใช้สีบอกอะไร

| สี/ป้าย | ความหมาย |
|---|---|
| 🔵 แถวสีฟ้า + AUTO badge | HIBE/VERP/UNBW ตัดอัตโนมัติ |
| ⚪ แถวสีขาว | ROH ต้องกรอกเอง |
| 🟠 "ไม่พบใน Master" (ส้ม) | Material code ไม่มีใน master — แจ้ง Admin |
| 🟠 "ไม่มี Stock" (ส้ม) | ยอดคงเหลือ = 0 — แจ้ง LD/Doc |
| ⚫ "—" (เทา) | AUTO row — ไม่ต้องเลือก Lot |

---

## 🆘 เมื่อมีปัญหา — Quick Fix

| อาการ | ทำก่อน | ถ้ายังไม่หาย |
|---|---|---|
| ไม่เห็น WO ที่ตัวเองต้องมิกซ์ | Refresh หน้า · เช็ค role | Operator ยัง start ไม่ครบ / QA ยังไม่อนุมัติ |
| "ไม่พบ BOM สำหรับสินค้านี้" | เช็ค product code ตรงไหม | แจ้ง PC / Admin อัปโหลด BOM |
| ROH เห็น "ไม่มี Stock" | เช็คว่ามีถุง lot อื่นในสต๊อกไหม | แจ้ง LD/Doc รับของก่อน |
| AUTO row ปริมาณดูผิด | คำนวณ BOM × actual ด้วยมือเทียบ | Screenshot + แจ้ง PC ตรวจ BOM |
| กด "ยืนยัน" แล้ว error | ตรวจว่ากรอก Lot ครบทุก ROH ที่ tick | Screenshot + แจ้ง IT |
| ชั่งของเสียแล้วเลือกประเภทผิด | อย่ากด save ซ้ำ | แจ้ง PC ให้แก้ในระบบ |

---

## ⛔ ข้อห้าม — อย่าทำ

1. ❌ **อย่ากรอก Qty มั่ว** — ROH ต้องเป็นค่าจริงจากการชั่ง ไม่ใช่ค่า BOM
2. ❌ **อย่าเลือก Lot ผิด** — trace back ไม่ได้ ถ้าเจอ defect
3. ❌ **อย่ากด "ใช้" HIBE/VERP/UNBW ทั้งที่ไม่ได้ใช้จริง** — Stock หายผิด
4. ❌ **อย่าให้กะถัดไปกดแทน** — บัญชีใครใช้ audit ลงชื่อคนนั้น
5. ❌ **อย่าชั่งเสียปลายกะทีเดียว** — ระบบเก็บ timestamp ผิด

---

## 📞 ช่องทางแจ้งปัญหา (ระหว่าง Pilot)

- **LINE Group**: GDTahara Pilot
- **P0 (batch save ไม่ผ่านทั้ง batch)**: โทร IT + PC ทันที
- **แจ้งปัญหาทั่วไป**: LINE + screenshot + เลข WO

### รูปแบบแจ้งปัญหา (Copy ไปวาง)
```
🐛 [แจ้งปัญหา CM Stock-out]
Role: CM Operator
เครื่อง: TAHARA-___
Product: __________
WO / Report ID: __________
Material code ที่มีปัญหา: __________
อาการ: (สั้น ๆ ทำอะไรอยู่ แล้วเกิดอะไรขึ้น)
Screenshot: (แนบรูป)
```

---

## ✅ ผ่าน Training นี้แล้ว คุณควรทำได้

- [ ] Login + เลือก WO จาก MachineSelectGrid
- [ ] เข้าใจโครงสร้าง BOM table (7 คอลัมน์)
- [ ] แยก ROH (มือ) vs HIBE/VERP/UNBW (AUTO) ได้
- [ ] ตัด ROH — tick + เลือก Lot + กรอก Qty จริง
- [ ] ตรวจ AUTO row + แก้ตัวเลขถ้าใช้ไม่ตรง
- [ ] กด "ยืนยัน" batch submit ทั้งหมดพร้อมกัน
- [ ] ⚖️ ชั่งของเสีย — เลือกประเภทถูก + กรอกน้ำหนัก
- [ ] อ่านสีระบบได้ (ฟ้า AUTO / ส้ม ผิด / เทา no-action)
- [ ] รู้ว่าเจอปัญหาแล้วต้องแจ้งใครในช่องทางไหน

---

## 🎓 คำศัพท์ที่ต้องรู้

| ศัพท์ | เต็มคำ | ความหมาย |
|---|---|---|
| **ROH** | Raw Material | เม็ดพลาสติกดิบ · **ต้องระบุ Lot** |
| **HIBE** | Halbfabrikat / Aux material | สารเสริม / additive · **AUTO** |
| **VERP** | Verpackung | Packaging material · **AUTO** |
| **UNBW** | Non-valuated | วัตถุดิบไม่คิดมูลค่า · **AUTO** |
| **FERT** | Fertigerzeugnis | Finished Goods (FG) — ไม่เกี่ยวกับ CM |
| **HALB** | Halbfabrikat | Semi-FG — ไม่เกี่ยวกับ CM |
| **isScrap** | Scrap flag | ระบบซ่อนแถวนี้ไว้แล้ว |
| **BOM** | Bill of Materials | สูตรวัตถุดิบต่อ 1 FG |
| **Stock-out** | เบิกจ่ายวัตถุดิบ | หักลด material stock |

---

**เอกสารเวอร์ชัน**: v1.0 · 31 กรกฎาคม 2026
**ระบบเวอร์ชัน**: Phase 1 · commit `bb6da49` (W18 CM Auto Stock-out)
**เขียนสำหรับ**: GDTAHARA MES Pilot · Toyo Seikan (Thailand)
