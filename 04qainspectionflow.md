# 04 — QA: บันทึกของเสียจากการสุ่มตรวจ

**สำหรับ**: QA Inspector (คนสุ่มตรวจคุณภาพระหว่างเดินเครื่อง)
**ใช้เวลาเรียน**: ~15 นาที
**ต้องมีก่อน**: บัญชี user + role = QA

---

## ⚠️ Scope ปัจจุบัน (Phase 1)

QA module ตอนนี้ทำได้ **1 อย่างหลัก**:
- ✅ **บันทึกของเสีย (NG) จากการสุ่มตรวจ** ระหว่างเดินเครื่อง (source = `QA_Process`)

**สิ่งที่ยังไม่มี** (จะเพิ่ม Phase 2 ตามแผน):
- ❌ Setup Approval (Approve งาน Setup ก่อน Operator เริ่มผลิต)
- ❌ In-Coming Inspection (IQC) form
- ❌ Out-Going Inspection (OQC) form
- ❌ NCR / Trouble Report workflow (ตอนนี้ยัง paper)

---

## ภาพรวม — งานของ QA ในกะ pilot

```
🌅 เริ่มกะ
  ↓
  1. Login → เห็นรายการใบสั่งผลิตที่ active
  2. คลิก WO ที่จะไปสุ่มตรวจ
  ↓
🔍 ระหว่างสุ่มตรวจ (ทำวนเรื่อย ๆ)
  ↓
  3. พบของเสีย → เลือกประเภท NG → กรอกจำนวน → บันทึก
  4. ตรวจประวัติของ WO นี้ (แก้ไข/ลบได้ถ้าเป็นของตัวเอง)
  ↓
🌆 จบกะ (ข้อมูลทั้งหมดไหลเข้า BlowDailyReport อัตโนมัติ)
```

---

## 1️⃣ Login + เข้าหน้ารายการ WO

### ขั้นตอน
1. Login เข้าระบบ
2. หน้า Dashboard แสดง **"เลือกใบสั่งผลิต"** (ทำนอง MachineSelectGrid)
3. เห็นเฉพาะ WO ที่ **active อยู่ในระบบ** (ไม่เห็น WO ที่ยังไม่เริ่ม หรือปิดไปแล้ว)
4. คลิกเครื่อง/WO ที่จะไปสุ่ม

### ⚠️ ถ้าไม่เห็น WO
- Operator ยังไม่กด Start RunCard → รอ Operator เริ่ม
- WO ผิดกะ → เช็ค PC
- Refresh หน้า (F5)

---

## 2️⃣ หน้า "บันทึกผลการตรวจสอบคุณภาพ"

### สิ่งที่เห็น
- ปุ่ม **← กลับไปหน้ารายการ** (ซ้ายบน)
- หัวข้อ **"บันทึกผลการตรวจสอบคุณภาพ"**
- Header: `เครื่องจักร:` และ `ผลิตภัณฑ์:` (จาก report)
- Section 1: **"บันทึกของเสีย (NG) จากการสุ่มตรวจ"** — grid ของประเภท NG ให้เลือก
- Section 2 (ล่าง): **"ประวัติการตรวจสอบคุณภาพ"** — ประวัติที่บันทึกไปแล้ว
  - แสดง count เช่น `แสดงประวัติ (5 รายการ)` / `ซ่อนประวัติ`

---

## 3️⃣ บันทึก NG จากการสุ่มตรวจ

### ขั้นตอน
1. **เลือกประเภทของเสีย** จาก grid (แต่ละปุ่ม = 1 NG Type)
   - แต่ละประเภทมี label ภาษาไทย
   - รายการ NG Types มาจาก master data (Admin ตั้งไว้)
2. Modal เด้ง — หัวข้อ `บันทึก: <ชื่อประเภท NG>`
3. กรอก **จำนวนที่พบ** (ชิ้น)
4. กด **บันทึก**
5. Alert แสดง "บันทึกของเสียสำเร็จ"
6. ประวัติ Section 2 update ทันที (ไม่ต้อง refresh)

### ⚠️ ข้อควรระวัง
- **จำนวนต้อง > 0** — ถ้ากรอก 0 ระบบจะ error
- **ห้ามใส่ตัวติดลบ**
- ห้ามใส่ทศนิยม — นับเป็นชิ้นเท่านั้น
- **บันทึกทุกครั้งที่เจอ** อย่ารวมชิ้นแล้วบันทึกทีเดียว (ระบบเก็บ timestamp ผิด)

### รายละเอียดที่ระบบเก็บ (auto)
- Report ID (WO ปัจจุบัน)
- NG Type ID (ประเภทที่เลือก)
- User (คุณ — จาก JWT token)
- Timestamp (เวลากด save)
- **Source = `QA_Process`** — แยกจาก Operator/Shift Leader ที่บันทึก NG ด้วย
- Quantity

---

## 4️⃣ ดูประวัติของ WO นี้

### ขั้นตอน
1. Scroll ลงหา **"ประวัติการตรวจสอบคุณภาพ"**
2. กดปุ่ม **แสดงประวัติ (N รายการ)** — พับ/กาง
3. ตารางแสดง 5 คอลัมน์:

| คอลัมน์ | ข้อมูล |
|---|---|
| วันที่/เวลา | Timestamp บันทึก |
| ผู้ตรวจ | Username ผู้บันทึก |
| ประเภทของเสีย | Label ภาษาไทย |
| จำนวน | `N ชิ้น` |
| จัดการ | ปุ่ม **✏️ แก้ไข** (เฉพาะของตัวเอง) |

### สิ่งที่ควรทราบ
- **เห็นประวัติของทุกคน** ที่บันทึก NG ในกะนี้ (source = QA_Process)
- **แก้ไขได้เฉพาะของตัวเอง** (IDOR protected — ระบบเช็ค username)
- ถ้าเห็นประวัติที่คนอื่นบันทึกและผิด → **แจ้ง PC/Shift Leader** ไปแก้ (คุณแก้ไม่ได้)

---

## 5️⃣ แก้ไข / ลบ NG Log (ของตัวเอง)

### แก้จำนวน
1. กด **✏️ แก้ไข** ที่แถวของตัวเอง
2. Modal เด้ง — pre-fill จำนวนเดิม
3. แก้ตัวเลขใหม่ → กด **บันทึก**
4. Alert "แก้ไขจำนวนสำเร็จ"

### ลบ (ใส่ 0 = ลบ)
1. กด **✏️ แก้ไข**
2. Modal เด้ง
3. **ใส่ 0** → กด **บันทึก**
4. Alert "ลบรายการสำเร็จ"

### ⚠️ ข้อควรระวัง
- **ลบแล้วย้อนไม่ได้** — audit log เก็บว่าใครลบเมื่อไหร่ แต่ค่าเดิมหายจากรายงาน
- ถ้าเห็น "จำนวนต้องไม่ติดลบ (ใส่ 0 หากต้องการลบ)" = พิมพ์เลขติดลบ ระบบไม่ยอม
- **ถ้าไม่ใช่ของตัวเอง** ระบบจะ error `"คุณสามารถแก้ไขได้เฉพาะรายการที่ตัวเองบันทึกเท่านั้น"`
- **ถ้าเป็น NG log จาก source อื่น** (Operator / Shift Leader) — แก้ไม่ได้เลยแม้เป็นของตัวเอง

---

## 6️⃣ ข้อมูลนี้ไปโผล่ที่ไหน

พอบันทึก NG แล้ว ข้อมูลไหลเข้า:

| หน้า | เห็นอะไร | ใครดู |
|---|---|---|
| **BlowDailyReport Section 5** | Grouped by type + description + source · sum · sort desc | PC |
| **Historical Reports** | ประวัติ NG ต่อ WO | PC · Doc |
| **Variance Report** | เทียบ target vs NG rate | PC |
| **Audit Logs** | Every action (CREATE/UPDATE/DELETE) | Admin |

**QA-recorded (source=QA_Process)** vs **Operator-recorded** vs **Shift Leader-recorded** — ระบบแยกให้ในรายงาน

---

## 🆘 เมื่อมีปัญหา — Quick Fix

| อาการ | ทำก่อน | ถ้ายังไม่หาย |
|---|---|---|
| ไม่เห็น WO ที่จะไปสุ่ม | Operator ยังไม่ start RunCard | รอ Operator เริ่ม + Refresh |
| กรอกจำนวนแล้ว "กรุณากรอกจำนวนที่ถูกต้อง" | ตัวเลข > 0 · ไม่ใช่ทศนิยม | ลองใส่ int ปกติ |
| แก้ไขไม่ได้ "เฉพาะรายการที่ตัวเอง" | เช็คว่า log นั้นใครบันทึก | แจ้ง PC ให้ user คนนั้นแก้ |
| แก้ไข NG ของ Operator ไม่ได้ | เพราะ source ≠ QA_Process | แจ้ง Shift Leader ให้แก้ |
| เห็นประวัติซ้ำ | อาจกด save 2 ครั้ง | ✏️ แก้ไข → 0 → ลบ 1 รายการ |
| ประเภท NG ไม่มีที่ต้องการ | เช็ค list ให้ครบ | แจ้ง Admin เพิ่ม NG Type |

---

## ⛔ ข้อห้าม — อย่าทำ

1. ❌ **อย่าบันทึกรวมกันเป็นก้อน** — เจอ 3 ชิ้นเวลา 10 โมง / 5 ชิ้นเวลาบ่าย 2 → บันทึก 2 ครั้ง ไม่ใช่ 8 ชิ้นทีเดียว
2. ❌ **อย่าใช้ account คนอื่น** — audit ล้มเหลว
3. ❌ **อย่ากรอกเดา** — ถ้ายังนับไม่ครบ กรอกที่เห็นก่อน แล้วบันทึกเพิ่มทีหลัง
4. ❌ **อย่าลบ NG ของกะที่ผ่านไปแล้ว** — รายงานไปที่ PC แล้ว อาจกระทบ KPI

---

## 📞 ช่องทางแจ้งปัญหา (ระหว่าง Pilot)

- **LINE Group**: GDTahara Pilot
- **แจ้งปัญหา P0 (บันทึก NG ไม่ได้ทั้งกะ)**: โทร IT + PC ทันที
- **แจ้งปัญหาทั่วไป**: LINE + screenshot + เลข WO/Report

### รูปแบบแจ้งปัญหา (Copy ไปวาง)
```
🐛 [แจ้งปัญหา QA]
Role: QA Inspector
เครื่อง: TAHARA-___
WO / Report ID: __________
NG Type: __________
อาการ: (สั้น ๆ ทำอะไรอยู่ แล้วเกิดอะไรขึ้น)
Screenshot: (แนบรูป)
```

---

## ✅ ผ่าน Training นี้แล้ว คุณควรทำได้

- [ ] Login + เลือก WO ที่จะไปสุ่มตรวจ
- [ ] บันทึก NG แต่ละครั้งที่พบ (ไม่รวมก้อน)
- [ ] เลือกประเภท NG ถูก + กรอกจำนวนถูก
- [ ] ดูประวัติของ WO นี้
- [ ] แก้ไข NG ของตัวเอง (ถ้ากรอกผิด)
- [ ] ลบ NG ของตัวเอง (ใส่ 0)
- [ ] เข้าใจว่าแก้ของคนอื่น / source อื่น ไม่ได้
- [ ] รู้ว่าข้อมูลไปโผล่ที่ BlowDailyReport / Variance / Historical

---

## 🎓 คำศัพท์ที่ต้องรู้

| ศัพท์ | เต็มคำ | ความหมาย |
|---|---|---|
| **NG** | No Good | ของเสีย |
| **NG Type** | - | ประเภทของเสีย (master data · Admin ตั้ง) |
| **Source** | - | ที่มาของการบันทึก · QA_Process / Operator / Shift Leader |
| **QA_Process** | - | source ที่ QA บันทึกเอง (ไม่ใช่ operator log) |
| **NgLog** | - | 1 record ของการพบของเสีย |
| **NgLogRepository** | - | database table `ng_logs` |
| **IQC** | In-Coming QC | ตรวจรับวัตถุดิบเข้า · **Phase 2** |
| **OQC** | Out-Going QC | ตรวจก่อนส่งลูกค้า · **Phase 2** |
| **IPQC** | In-Process QC | ตรวจระหว่างผลิต · **บางส่วน** ใน parameter_records |
| **FPI** | First Piece Inspection | ตรวจตัวแรกหลัง Setup · **Phase 2** |
| **NCR** | Non-Conformance Report | ยัง paper · **Phase 2** |
| **CAPA** | Corrective + Preventive Action | **Phase 3** |

---

**เอกสารเวอร์ชัน**: v1.0 · 31 กรกฎาคม 2026
**ระบบเวอร์ชัน**: Phase 1 · commit `bb6da49`
**เขียนสำหรับ**: GDTAHARA MES Pilot · Toyo Seikan (Thailand)
